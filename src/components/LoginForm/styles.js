import styled from 'styled-components';

export const StyledGuestLogin = styled.section`
    margin-bottom: 1rem;

    button {
        width: 100%;
        background: #0000008f;
        border: 1px solid rgba(0, 0, 0, 0.58);
        cursor: pointer;
        font-size: 1rem;
        padding: 0.5rem;
        color: white;
    }
`;
